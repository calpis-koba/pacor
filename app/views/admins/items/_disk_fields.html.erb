<p>ディスク</p>

<div class="nested-fields">

  <%= link_to_remove_association "ディスクを削除", f, class: "btn btn-danger" %>
  
    <div class="songs">
      
      <%= f.fields_for :songs do |song| %>
        <%= f.label "曲" %>
        <%= render "song_fields", f: song %>
      <% end %>
      
      <div class="links">
        <%= link_to_add_association "曲を追加", f, :songs, class: "btn btn-success" %>
      </div>
      
    </div>
<div